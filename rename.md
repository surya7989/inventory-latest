# Project Renaming Functionality

## Overview
This project has been renamed from **StockMaster** to **Nexarats**.

## Changes Made
- All occurrences of `StockMaster` in the codebase have been replaced with `Nexarats`.
- The main branding and titles now reflect **NexaratsINV** or **Nexarats**.
- Local storage keys have been updated where applicable (e.g., `nx_admin_profile`, `nx_gst_config`).

## Verification
- Usage of the new name can be verified in:
  - `src/layouts/Sidebar.tsx` (Sidebar Title)
  - `src/layouts/Header.tsx` (Dashboard Titles)
  - `README.md` (Project Title and Description)
